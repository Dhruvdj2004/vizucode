// The OS module uses the shared content model; these aliases keep the topic
// files readable.

export type { Block, Rich, Tone } from '../content/types';
export type { Section as OsSection, QA as OsQA, Topic as OsTopic } from '../content/types';
