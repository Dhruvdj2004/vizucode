// The SQL module uses the shared content model; these aliases keep the topic
// files readable.

export type { Block, Rich, Tone } from '../content/types';
export type { Section as SqlSection, QA as SqlQA, Topic as SqlTopic } from '../content/types';
