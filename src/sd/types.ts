// The System Design module uses the shared content model; these aliases keep
// a `SdTopic` naming convention in the topic files, matching the other
// theory modules (DBMS, OS, OOPs, CN, SQL).

export type { Block, Rich, Tone } from '../content/types';
export type { Section as SdSection, QA as SdQA, Topic as SdTopic } from '../content/types';
