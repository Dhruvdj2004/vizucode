// The OOP module uses the shared content model; these aliases keep the topic
// files readable.

export type { Block, Rich, Tone } from '../content/types';
export type { Section as OopSection, QA as OopQA, Topic as OopTopic } from '../content/types';
