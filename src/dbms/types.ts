// The DBMS module uses the shared content model; these aliases keep the
// existing `DbmsTopic` naming in the 25 topic files.

export type { Block, Rich, Tone } from '../content/types';
export type { Section as DbmsSection, QA as DbmsQA, Topic as DbmsTopic } from '../content/types';
