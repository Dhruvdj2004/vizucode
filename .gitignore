# dependencies & build output — never commit or deploy source-adjacent artifacts
node_modules/
dist/
server/dist/
*.tsbuildinfo

# environment & secrets
.env
.env.*

# OS noise
.DS_Store

# Local Netlify folder
.netlify

# scratch output from the scripts/verify-*.mjs headless checks — each run keeps
# its own throwaway Chrome profile, which is hundreds of MB
.verify-chrome-profile*/
.verify-screenshots/
